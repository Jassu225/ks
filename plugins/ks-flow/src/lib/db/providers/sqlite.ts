// lib/db/providers/sqlite.ts — FUTURE provider (v2), documented slot only.
//
// Planned shape:
//   writer = better-sqlite3 in WAL mode under $CLAUDE_PLUGIN_DATA.
//   source = a Next route owned by this provider + a light poll from the board.
//
// Not implemented in v1. createProvider() throws for db_provider="sqlite".
export {};
