#!/bin/bash
# KS Start Ticket wrapper
# Usage: ./ks-start-ticket <issue-url> [output-path]

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ORIGINAL_DIR="$(pwd)"
cd "$SCRIPT_DIR"

KS_ORIGINAL_DIR="$ORIGINAL_DIR" npx tsx ks-start-ticket.ts "$@"
